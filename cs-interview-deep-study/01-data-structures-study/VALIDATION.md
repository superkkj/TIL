# 자료구조 Study Edition 검증 기록

> 검증 대상: `01-data-structures-study/`
> 원본 목차: `../01-data-structures/00-index.md`
> 세부 원본 목차: `../01-data-structures/01-02-hashmap/00-index.md`
> 적용 규칙: `$study` Ailey & Bailey Fact-Grounded Edition
> 검증 시각: 2026-07-22 14:48 KST

## 작업 범위

이번 작업은 원본 자료구조 목차의 1.1~1.43만 대상으로 한다. 네트워크, 운영체제, Java, 데이터베이스, Spring, 운영 설계 목차는 이 검증 범위에 포함하지 않았다.

원본 목차의 단원 번호, 주제, 파일명은 학습본에서 같은 순서와 같은 파일명으로 유지했다. HashMap의 1.2~1.10은 원본처럼 하위 폴더로 묶었다.

각 학습 문서는 다음 구성 요소를 가진다.

1. Ailey 학습 코치 설명
2. 핵심 원리와 쉬운 설명
3. 내부 구조·불변식·실패 지점
4. 성능과 선택 기준
5. Bailey 면접 꼬리질문
6. 요약과 다음 학습 링크
7. 사실(F)과 모름(U)
8. 작성 시각

## AS-IS 확인

학습본 43개는 원본 43개와 파일 단위로 대응했다. 초기 검사에서는 Study 규칙이 금지한 표현 134건과 생략을 나타내는 점 세 개 표기 11건이 확인됐다.

금지 표현은 기술 의미를 유지하는 직접 서술형으로 바꿨다. 영어 트리 형태 명칭은 정 이진 트리, 완전 이진 트리, 포화 이진 트리라는 한국어 명칭으로 정리했다. 예제 안의 생략 표시는 명시적인 범위 설명이나 subtree 표기로 바꿨다.

## 검증 결과

| 검사 항목 | 결과 | 확인 방법 |
|---|---:|---|
| 원본 단원 문서 수 | 43개 | `find 01-data-structures ...` |
| Study 단원 문서 수 | 43개 | `find 01-data-structures-study ...` |
| 원본·Study 파일명 차이 | 0건 | 정렬 후 `comm -3` |
| 사실(F) 섹션 누락 | 0건 | 전체 Markdown 제목 검사 |
| 모름(U) 섹션 누락 | 0건 | 전체 Markdown 제목 검사 |
| 웨이드님 호칭 누락 | 0건 | 전체 문서 문자열 검사 |
| Bailey 질문 누락 | 0건 | 전체 문서 문자열 검사 |
| 문서당 출처 표기 10개 미만 | 0건 | `[출처:` 개수 검사 |
| Study 금지 표현 잔여 | 0건 | 규칙의 한국어·영어 금지 목록 전체 검색 |
| 생략용 점 세 개 표기 잔여 | 0건 | 전체 Markdown 정규식 검색 |
| 깨진 로컬 Markdown 링크 | 0건 | 링크 대상 파일 존재 검사 |
| 원본의 근거 섹션 누락 | 0건 | 원본 43개 `## 0. 기준과 근거` 검사 |
| 원본의 공식 HTTPS 출처 누락 | 0건 | 원본 43개 URL 검사 |
| 공식 출처 URL 응답 | 41개 모두 HTTP 200 | Oracle, OpenJDK, NIST URL 직접 요청 |

## 공식 근거 범위

원본 43개가 인용한 출처의 도메인은 다음 세 곳이다.

1. Oracle Java Language Specification과 Java SE 17 API 문서
2. OpenJDK 17u 공식 소스 저장소
3. NIST Dictionary of Algorithms and Data Structures

Java 배열의 객체성, component type, 0부터 시작하는 index, runtime 저장 검사와 `ArrayStoreException`은 Java Language Specification 17의 Chapter 10에서 직접 확인했다.

Java Collections Framework의 패키지 범위와 컬렉션 구현 목록은 Oracle Java SE 17 `java.util` 공식 문서에서 직접 확인했다.

## 검증 한계

URL 응답 검사는 공식 문서가 실제로 열리는지를 검증한다. 이 검사는 학습본 약 2만 8천 줄의 모든 문장을 공식 원문의 문장과 의미 단위로 일대일 대조한 결과가 아니다.

세부 주장별 검증 상태는 각 문서 마지막의 사실(F)과 모름(U)을 기준으로 한다. 모름(U)에 적힌 구현 세부, 환경별 측정값, 원본이 제공하지 않은 범위는 이 학습본의 확인 범위 밖이다.

---

## 사실 (F)

F1. 원본 목차와 Study 학습본에는 각각 43개 단원 문서가 있고 파일명 차이는 0건이다. [출처: 2026-07-22 로컬 파일 전수검사]

F2. 원본 43개에는 모두 `## 0. 기준과 근거`와 HTTPS 출처가 있다. [출처: 2026-07-22 로컬 원본 문서 전수검사]

F3. 원본에서 추출한 Oracle·OpenJDK·NIST 공식 URL 41개는 검증 시점에 모두 HTTP 200으로 응답했다. [출처: 2026-07-22 `curl -L` 응답 코드 검사]

F4. Study 학습본에는 사실(F), 모름(U), 웨이드님 호칭, Bailey 질문이 모두 있으며 금지 표현과 깨진 로컬 링크는 확인되지 않았다. [출처: 2026-07-22 전체 Markdown 구조·문자열·링크 검사]

## 모름 (U)

U1. 공식 사이트가 이후에도 같은 URL과 내용을 유지하는지는 이 검증 기록이 보장하지 않는다.

U2. 각 문장의 의미 단위 공식 원문 대조는 수행하지 않았다. 세부 범위는 각 문서의 사실(F)과 모름(U)을 따른다.

[2026.07.22 (수) 14:48:40]
